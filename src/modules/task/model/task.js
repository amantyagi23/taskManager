class Task{
    constructor (taskId,title,desc,createdAt){
        this.taskId = taskId;
        this.title = title;
        this.desc = desc;
        this.createdAt = createdAt;
    }
}

export default Task